import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Next.js automatically detects src/app directory in v15
  // No additional configuration needed for src directory
};

export default nextConfig;
